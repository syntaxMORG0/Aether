# Aether

Lightweight ARM based kernel for embedded devices

# Build Instructions

## Prerequisites

### Arch Linux

Install `pacman -S arm-none-eabi-gcc arm-none-eabi-binutils`
For testing `pacman -S qemu-system-arm`
